# Vercel — Import this file (vercel.env at repo root)

DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@YOUR_HOST.neon.tech/neondb?sslmode=require
JWT_SECRET=your-long-random-secret-here

# Admin login (auto-created in database on first API call):
# Username: ENGbarre  OR  Barre@gmail.com
# Password: Barre@55

# Vercel steps:
# 1. Project → Settings → Environment Variables
# 2. Click "Import .env" → select vercel.env
# 3. Enable: Production + Preview + Development (all three)
# 4. Redeploy after saving

# Test after deploy:
# https://YOUR-SITE.vercel.app/api/health  → should show database connected
# https://YOUR-SITE.vercel.app/login       → admin login
