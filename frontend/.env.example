# Local development only (optional — browser uses /backend-api proxy)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Production: backend Vercel URL (no trailing slash)
# Example: https://mohamed-barre-api.vercel.app
BACKEND_URL=http://localhost:3001
